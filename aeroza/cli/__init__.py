"""Aeroza command-line entry-points.

Each subcommand lives in its own module so the surface area stays
discoverable. Invoke either via the package (``python -m aeroza.cli ...``)
or via the registered console script (``aeroza-ingest-alerts``).
"""
